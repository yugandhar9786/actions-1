name: My-First-Workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        -name: welcome message
         run: echo "my first github actions"
        -name: list files
         run: ls
        -name: read the file
         run: cat README.md

